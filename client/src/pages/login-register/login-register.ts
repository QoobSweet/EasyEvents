import { LitElement, html, customElement, property } from 'lit-element';
import { style } from './login-register-css';

@customElement('login-register')
export class LoginRegisterPage extends LitElement {
  
  
  render() {
  return html`
    `;
  }
}
